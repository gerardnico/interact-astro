# Page templating

## Container

* [class](https://getbootstrap.com/docs/5.3/layout/containers/)
* and optional `max-width` configuration

This container is applied on all three stacked layout component:

* in the navbar
* on the `page core` except when the page is `landing`
* in the footer