# Debug

## How to debug when used as local dependency

When this module is used as local dependency in another project,
you open the other project in your IDE, go to the `node_modules` project and
set the keyword `debugger` in the dependency file.
