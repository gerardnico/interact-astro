---
import type {LayoutProps} from "../../types";
import Holy from "./Holy.astro";
const props = Astro.props as LayoutProps;
---
<Holy frontmatter={props.frontmatter} headings={props.headings}>
    <slot/>
</Holy>
